<h1>List of Games</h1>

<table border="4" class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>Player 1</th>
      <th>Player 2</th>
      <th>Winner</th>
      <th>Status</th>
      <th>Best Of</th>
      <th colspan="2">Options</th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.player_1.name %></td>
        <td><%= game.player_2.name %></td>

        <% if !game.finished? %>
          <td></td>
        <% elsif game.winner.nil? %>   <!--Winner Column-->
          <td>Tie!</td>
        <% else %>
          <td><%= game.winner.name %></td>
        <% end %>

        <% if game.finished? %>   <!--Status Column-->
          <td>Finished</td>
        <% else %>
          <td>In Progress</td>
        <% end %>
        <td><%= game.round_count %></td>

        <% if game.finished? %>  <!--Option Column 1-->
          <td><%= link_to 'Results', rounds_url(game_id: game.id) %></td>
        <% else %>
          <td><%= link_to 'Play', rounds_url(game_id: game.id)  %></td>
        <% end %>
        <td><%= link_to 'Delete', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'New Game', new_game_path, :method => "get" %>
