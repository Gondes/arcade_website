<!DOCTYPE html>
<html>
  <head>
    <title>Arcade</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Arcade</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Manage Users", users_url  %></li>
            <li><%= link_to "Manage Games", games_url  %></li>
            <li><%= link_to "FAQ", faqs_url  %></li>
            <% if account_signed_in? %>
              <% if current_account.user_id.nil? %>
                <li><%= link_to "Profile", new_user_path %>
              <% else %>
                <li><%= link_to (User.find current_account.user_id).user_name, user_path(current_account.user_id) %></li>
              <% end %>
            <% end %>
          </ul>

          <p class="navbar-text pull-right">
            <% if account_signed_in? %>
              <% if current_account.user_id.nil? %>
                Logged in as <strong><%= current_account.email %></strong>.
              <% else %>
                Logged in as <strong><%= (User.find current_account.user_id).user_name %></strong>.
              <% end %>
              <%= link_to 'Edit profile', edit_account_registration_path, :class => 'navbar-link' %> |
              <%= link_to "Logout", destroy_account_session_path, method: :delete, :class => 'navbar-link'  %>
            <% else %>
              <%= link_to "Sign up", new_account_registration_path, :class => 'navbar-link'  %> |
              <%= link_to "Login", new_account_session_path, :class => 'navbar-link'  %>
            <% end %>
          </p>
        </div>
      </div>
    </div>

    <div class="container">
    	<div class="row">

        <% if notice %>
          <p class="alert alert-success"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
        
    		<div class="col-lg-9" id="main"><%= yield %></div>
    		<div class="col-lg-3" id="side_bar">
    			<h2>About Us</h2>
    			<p>This is just a filler text that will be later replace with actual information that will be useful for the person(s) using this website. Until then, please put up with this completely verbose and useless block of rambling text. I thank you for your understanding.</p>
    		</div>
    	</div>
    </div>

    <div class="footer">
      <hr>
      <p>&copy; Company 2014</p>
    </div>
  </body>
</html>
